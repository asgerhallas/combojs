{
  "name": "combojs",
  "version": "0.0.1",
  "description": "A combobox for intelligent, responsive search and filtering.",
  "main": "combojs.js",
  "directories": {
    "example": "examples",
    "test": "tests"
  },
  "scripts": {
    "clean": "del .\\lib\\build /Q& del .\\tests\\results /Q& del .\\tests\\build /Q",
    "stylus": "stylus -m -w -o lib/build lib/combo.styl",
    "coffee_test": "coffee -o lib/build -cmw lib/",
    "coffee_lib": "coffee -o tests/build -cmw tests/",
    "simple_nightwatch": "nightwatch --config ./tests/nightwatch.json",
    "nightwatch": "nightwatch --config ./tests/nightwatch.json -e ie",
    "_pretest": "npm run start",
    "test": "npm run nightwatch",
    "testwatch": "watch \"npm test\" ./tests/build/ --wait=10",
    "start": "npm run clean& start npm run stylus& start npm run coffee_lib& start npm run coffee_test& start npm run testwatch",
    "startce": "npm run clean -new_console& npm run stylus -new_console& npm run coffee_lib -new_console& npm run coffee_test -new_console& npm run testwatch -new_console"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/asgerhallas/combojs"
  },
  "keywords": [
    "searchbox",
    "combobox",
    "reponsive",
    "filter"
  ],
  "author": "Asger Hallas",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/asgerhallas/combojs/issues"
  },
  "homepage": "https://github.com/asgerhallas/combojs",
  "dependencies": {
    "jquery": "^2.1.1",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "chai": "^1.10.0",
    "should": "^4.3.0",
    "stylus": "^0.49.3"
  }
}
